name: Sync to IONOS VPS

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  sync:
    runs-on: ubuntu-latest
    steps:
      - name: SSH and Sync
        uses: appleboy/ssh-action@master
        with:
          host: 216.250.127.169
          username: root
          password: vAUL03juyPr1hu
          script: |
            # Sync update - Friday, June 5, 2026
            cd /root/gross-bros-v7 && git fetch origin && git reset --hard origin/main && npm install && (pm2 delete all || true) && pm2 start ecosystem.config.js --update-env || (pm2 start services/guardian-ai/vtuber_engine.py --interpreter python3 && pm2 start services/guardian-signal-receiver/app.py --interpreter python3 && pm2 start server.js) && pm2 save
