name: Sync to IONOS VPS

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  sync:
    runs-on: ubuntu-latest
    steps:
      - name: SSH and Sync
        uses: appleboy/ssh-action@master
        with:
          host: 216.250.127.169
          username: root
          password: vAUL03juyPr1hu
          script: |
            # Sync update - Sunday, June 7, 2026
            cd /root/gross-bros-v8 && git fetch origin && git reset --hard origin/main && npm install && pm2 restart all || pm2 start server.js && pm2 save
