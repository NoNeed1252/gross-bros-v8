const fetch = require("node-fetch"); module.exports = async (req, res) => { try { const response = await fetch("https://api.coingecko.com/api/v3/simple/price?ids=ripple&vs_currencies=usd"); const data = await response.json(); if (data.ripple && data.ripple.usd) { res.json({ xrp_price: parseFloat(data.ripple.usd), timestamp: new Date().toISOString() }); } else { throw new Error("Invalid response"); } } catch (error) { res.status(500).json({ error: "Failed to fetch price" }); } };