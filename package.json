{
  "name": "gross-bros-v8",
  "version": "1.0.1",
  "type": "module",
  "scripts": {
    "build": "mkdir -p public && cp index.html public/index.html && echo \"SYNC COMPLETE\""
  },
  "dependencies": {
    "xrpl": "^3.0.0"
  }
}